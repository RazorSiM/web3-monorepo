{
  "name": "web3-monorepo",
  "version": "0.0.0",
  "private": true,
  "scripts": {
    "lint:check": "turbo lint:check",
    "lint:fix": "turbo lint:fix",
    "build": "turbo build", 
    "test:unit": "turbo test:unit",
    "test:hardhat": "turbo test:hardhat",
    "test": "turbo lint:check test:unit test:hardhat",
    "dev:example-app": "turbo run dev --filter=example-app..."
  },
  "devDependencies": {
    "dotenv-cli": "^7.2.1",
    "turbo": "^1.10.7"
  }
}
